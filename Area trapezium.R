a<-as.numeric(readline(prompt="Enter first parallel side of trapezium = "))
b<-as.numeric(readline(prompt="Enter second parallel side of trapezium = "))
h<-as.numeric(readline(prompt="Enter height of trapezium = "))
area<-0.5*(a+b)*h
cat("Area = ",area)
